const funkyVariable = function(arg) { //function expression = anonymous function stored inside the variable to be called later
  return arg;
}

console.log(funkyVariable("Hello!")); //assigning function to a variable